# Приёмочные тесты

Тесты нумеруются сквозно по ТЗ §24. Этап считается закрытым, когда все его
тесты проходят.

## Этап 1 — базовый multiplayer

Запуск: PIE, `Number of Players = 2`, `Net Mode = Play Standalone`,
`Run Under One Process` **выключено**.

| # | Тест | Ожидаемый результат |
|---|---|---|
| 1.1 | Проект собирается | `Development Editor` компилируется без ошибок; в логе сборки `[CSFusion] Photon Fusion 3 SDK found -> CS_WITH_FUSION=1` |
| 1.2 | Подключение к Photon | В логе `Session state: None -> Connecting -> Connected -> JoiningRoom -> InRoom` |
| 1.3 | Два игрока в комнате | В обоих окнах `CSNetInfo` показывает `players=2` и одинаковое имя комнаты |
| 1.4 | Ровно один авторитет | В одном окне `authority=YES`, в другом `authority=no` |
| 1.5 | Спавн | Оба персонажа появились на разных `PlayerStart` |
| 1.6 | Репликация движения | Игрок A двигается — игрок B видит это в своём окне, и наоборот |
| 1.7 | First person | Свой персонаж — только руки и тень; чужой — полная модель |
| 1.8 | Движение | WASD, мышь, `Space` прыжок, `LeftShift` спринт (скорость 420 → 620), `LeftCtrl` присед (капсула 88 → 60, скорость 220) |
| 1.9 | Спринт не назад | Спринт не ускоряет движение назад и вбок (порог dot 0.5) |
| 1.10 | Фазы матча | Через 5 с после входа фаза меняется на `Warmup`, затем `InProgress`; смена видна в обоих окнах |
| 1.11 | Глобальный RPC | Сообщения «Warmup started» / «Round started» появляются в логе **обоих** клиентов, но отправляет их только авторитет |
| 1.12 | Master migration | Закрыть окно-авторитет → во втором окне в логе `local player is NOW the authority`, фазы матча продолжают идти |
| 1.13 | Выход | `LeaveMatch()` возвращает состояние в `Connected`, персонаж удаляется у остальных (PlayerAttached) |
| 1.14 | Offline-путь | Запустить PIE с 1 игроком и не входить в комнату: `CSNetInfo` показывает `backend=Offline`, `authority=YES`, фазы матча идут, announcement'ы попадают в лог через прямой вызов `_Receive` |
| 1.15 | SDK обязателен | Временно переименовать `Plugins/PhotonFusion` → сборка падает с понятным `BuildException` и ссылкой на `docs/PHOTON_SETUP.md`, а не молча собирается в сломанном виде |

## Этап 2 — бой (план)

| # | Тест |
|---|---|
| 2.1 | У игрока при спавне HP=100, Armor=0, стартовый пистолет доступен, инвентарь пуст |
| 2.2 | Игрок A стреляет в игрока B |
| 2.3 | B получает урон; значение HP приходит от авторитета, а не считается локально |
| 2.4 | Headshot наносит множитель урона |
| 2.5 | B умирает при HP ≤ 0 |
| 2.6 | Клиент, отправивший запрос на выстрел чаще fire rate, получает отказ |
| 2.7 | B респавнится и снова имеет только стартовый пистолет |

## Этап 3–4 — инвентарь и лут (план, из ТЗ §24)

| # | Тест |
|---|---|
| 3.1 | На карте лежат AK-47, M4, SMG, Shotgun, Ammo, Grenades, Armor, Medkit |
| 3.2 | Подход к AK-47 показывает «AK-47 / Press E to pick up» |
| 3.3 | `E` → AK-47 попадает в инвентарь, предмет исчезает с карты |
| 3.4 | Два игрока жмут `E` одновременно — предмет получает ровно один |
| 4.1 | При смерти инвентарь B превращается в лут на земле, с разбросом |
| 4.2 | Стартовый пистолет **не** выпадает |
| 4.3 | Игрок A подбирает выпавший лут |
| 4.4 | B выходит из игры с AK в инвентаре → AK появляется на месте выхода |
| 4.5 | Другой игрок подбирает этот AK |
| 4.6 | Двойная обработка disconnect не создаёт второй набор предметов |
| 4.7 | Лут исчезает по таймеру, количество активных предметов ограничено |

## Результаты прогона v0.2.0-alpha (упакованный Win64 Development, два клиента)

| # | Тест | Результат |
|---|---|---|
| 1.2 | Подключение к Photon | ✅ оба клиента `InRoom`, регион `eu` |
| 1.4 | Ровно один авторитет | ✅ A: `NOW the authority`, B — нет |
| 1.8 | Движение | ✅ A 598.9 см, B 594.9 см; поворот 17.5° |
| 2.1 | Стартовое состояние | ✅ `Registered player N (hp 100, 12 rounds)` |
| 2.2 | Выстрел | ✅ A и B: `rounds 12 -> 11` (у B — через RPC к мастеру) |
| 2.3 | Урон от авторитета | ✅ `victim 2 hp 100 -> 78` (22 = урон в корпус без спада) |

Команда воспроизведения:

```bash
CSFusion.exe -windowed -room=test -cstestinput -cstestshoot
```

## Результаты прогона v0.3.0-alpha (упакованный билд, два клиента)

| # | Тест | Результат |
|---|---|---|
| 3.1 | На карте лежат предметы | ✅ `Authority spawned 16 map pickups` |
| 3.3 | Подбор AK-47 | ✅ `PICKUP OK` |
| — | Экипировка клавишей 2 | ✅ `EQUIP OK (AK-47, slot 0, rounds 30)` |
| — | Выстрел из оружия инвентаря | ✅ `rounds 30 -> 29` |
| — | Выброс по G | ✅ `DROP OK (in inventory 0, pickups 15 -> 16, back to starter yes)` |
| 3.4 | Одновременный подбор | ✅ оба нажали E в сетевое время 24.00: один `WON`, другой `LOST`, на земле 0 |

Ограничение теста 3.4: лог не доказывает, что оба запроса дошли до мастера
в одном кадре. «Ровно один победитель» гарантируется построением —
последовательной обработкой запросов и проверкой `IsAvailable()`.

```bash
CSFusion.exe -room=test -cstestloot -cstestcontest
```

## Результаты прогона v0.4.0-alpha (упакованный билд, два клиента)

| ТЗ | Тест | Результат |
|---|---|---|
| 4 | B умирает | ✅ `Player 2 killed by 1`, 5 выстрелов из пистолета |
| 5 | Инвентарь B превращается в лут | ✅ `victim inventory 2 -> 0, pickups 14 -> 16` |
| — | Стартовый пистолет не выпал | ✅ `starter kept yes` |
| 6 | A подбирает лут B | ✅ `took the victim's weapon (30 rounds kept)` |
| 7–8 | B респаунится только с пистолетом | ✅ `alive, hp 100, starter (12 rds), inventory 0` |
| 9–10 | B «падает» с AK — AK появляется на месте выхода | ✅ процесс убит, через ~8 с `dropped 2 pickups at V(X=-2260, Y=450)` |
| 11 | Другой игрок подбирает этот AK | ✅ `took the leaver's weapon -> CLAIM OK` |
| — | Двойное уведомление об уходе | ✅ живой прогон: второе уведомление Photon → `already handled - ignoring repeat` |
| — | Синтетический тест двойного выброса | ✅ удалён 3 раза, лут появился 1 раз (16 → 19 при 3 предметах) |
| — | Игроки не появляются в одной точке | ✅ исправлен баг стартового телепорта |

```bash
# смерть
CSFusion.exe -room=t -cstestkill        # клиент A (запустить первым)
CSFusion.exe -room=t -cstestgrab        # клиент B
# обрыв: B с -cstestgrab, A с -cstestwatchleave, затем убить игровой процесс B
# двойной выброс (оффлайн)
CSFusion.exe -noautoconnect -cstestdoubledrop
```

## Результаты прогона v0.5.0-alpha (упакованный билд, два клиента, всё через меню)

| Сценарий | Результат |
|---|---|
| Запуск без аргументов открывает главное меню, все 7 страниц рисуются | ✅ (`-cstestmenu`, скриншоты) |
| A: Create Session через меню → вход в матч | ✅ |
| B: Session Browser показывает комнату A (1/8, Open) через 3 с, JOIN из списка | ✅ |
| Quick Match на двух клиентах → одна и та же комната | ✅ (до v0.5.0 каждый создавал свою) |
| Экран инвентаря открывается/закрывается, фокус и курсор верные | ✅ |
| ESC-меню открывается/закрывается, курсор скрывается после закрытия | ✅ |
| Настройки применяются сразу: FOV 88 на камере, Interact переназначен на F в живой раскладке | ✅ |
| Убийство → запись в килл-фиде и хитмаркер у стрелка | ✅ |
| Leave Match → главное меню → повторный вход в ту же комнату | ✅ (второй матч прошёл все UI-тесты заново) |
| После выхода A роль Master Client перешла к B, B выбросил лут A | ✅ |

Замечание: в этом прогоне тест подбора у B (`-cstestgrab`) показал 0 предметов —
A убил B (по таймеру `-cstestkill`) в момент подбора. Это хореография теста,
подбор проверен в прогонах v0.3.0/v0.4.0 и не менялся.

```
# A
CSFusion.exe -cstestmenu=create:ROOM -cstestui -cstestkill -cstestleave=50 -cstestrejoin=ROOM
# B
CSFusion.exe -cstestmenu=browsejoin:ROOM -cstestgrab
# Quick Match (на обоих)
CSFusion.exe -cstestmenu=quick
```
